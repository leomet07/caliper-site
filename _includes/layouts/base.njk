<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <title>{{ title or metadata.title }}</title>
        <meta
        name="description" content="{{ description or metadata.description }}"/> <!-- capture the CSS content as a Nunjucks variable -->
        {% set css %}
        {% include "styles/globals.css" %}
        {% endset %}
        <!-- feed it through our cssmin filter to minify -->
        <style>
            {{css | cssmin | safe}}</style>
        <link rel="preload" as="font" href="/fonts/DecoCard/DecoCard.ttf" type="font/ttf"/>
        <link rel="icon" href="/assets/favicon.ico" type="image/x-icon">
    </head>
    <body>
        {% include "components/navbar.njk" %}
        <main id="main_content">
            {{ content | safe }}
        </main>
    </body>
</html>
